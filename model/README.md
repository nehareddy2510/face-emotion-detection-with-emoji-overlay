# Trained Model

This directory contains the trained CNN model used for facial emotion classification.

Files:
- `emotiondetector.json` → model architecture
- `emotiondetector.h5` → trained model weights
